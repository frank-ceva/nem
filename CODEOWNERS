# CODEOWNERS — defines ownership for review assignment
#
# Tool agents own their tool directories.
# Libs agent owns shared libraries, test infrastructure, and build config.
# Integration agent owns contracts, spec, and has review authority over all shared areas.

# Integration-owned (require integration agent review)
/docs/contracts/         @integration
/spec/                   @integration

# Libs-agent-owned (require libs agent + integration agent review)
/libs/                   @libs-agent @integration
/tests/conformance/      @libs-agent @integration
/Makefile                @libs-agent @integration

# Tool-owned
/tools/interpreter/      @interpreter-agent
/tools/compiler/         @compiler-agent
/tools/binder/           @binder-agent
/tools/simulator/        @simulator-agent
